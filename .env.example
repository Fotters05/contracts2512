# Database Connection Settings
# Copy this file to .env and configure your PostgreSQL connection

# Individual database parameters (used by Node.js parser)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=contract_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# Full connection string (used by C# application, auto-generated when you save settings)
DB_CONNECTION_STRING=Host=localhost;Port=5432;Database=MPT2512;Username=postgres;Password=1

# GitHub Auto-Update Settings (for private repositories)
# Get your Personal Access Token from: https://github.com/settings/tokens
# Click "Generate new token (classic)" and select scope: repo (Full control of private repositories)
GITHUB_OWNER=Fotters05
GITHUB_REPO=ContractTest
GITHUB_TOKEN=ghp_your_token_here
