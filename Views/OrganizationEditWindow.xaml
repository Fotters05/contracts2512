<ui:FluentWindow x:Class="Contract2512.Views.OrganizationEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Редактирование организации"
        WindowStartupLocation="CenterScreen"
        Height="600"
        Width="700"
        ExtendsContentIntoTitleBar="True">

    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackground" StartPoint="0 0" EndPoint="1 1">
            <GradientStop Color="#1E1B4B" Offset="0"/>
            <GradientStop Color="#0F172A" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="TextPrimary" Color="White"/>
        <SolidColorBrush x:Key="BorderColor" Color="#64748B"/>

        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#1E293B" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Background="Transparent"
                                        Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#8B5CF6"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#4B5563" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Color="#6B7280" Opacity="0.8"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопок управления окном -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Color="White" Opacity="0.1"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom TitleBar -->
        <Border Grid.Row="0" Background="#312E81" Height="30" MouseDown="TitleBar_MouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" 
                          Text="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Title}"
                          Foreground="White"
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"
                          FontSize="12"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="─" Style="{StaticResource WindowControlButton}" Click="MinimizeButton_Click"/>
                    <Button x:Name="MaximizeRestoreButton" Content="□" Style="{StaticResource WindowControlButton}" Click="MaximizeButton_Click"/>
                    <Button Content="✕" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Информация об организации" 
                          FontSize="20" 
                          FontWeight="SemiBold" 
                          Foreground="{StaticResource TextPrimary}" 
                          Margin="0,0,0,20"/>

                <TextBlock Text="Название организации *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="OrganizationNameTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="ФИО директора *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="DirectorFioTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="ОГРН (13 цифр) *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="OgrnTextBox" Style="{StaticResource DarkTextBoxStyle}" MaxLength="13" Margin="0,0,0,15"/>

                <TextBlock Text="ИНН (10 цифр) *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="InnTextBox" Style="{StaticResource DarkTextBoxStyle}" MaxLength="10" Margin="0,0,0,15"/>

                <TextBlock Text="КПП (9 цифр) *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="KppTextBox" Style="{StaticResource DarkTextBoxStyle}" MaxLength="9" Margin="0,0,0,15"/>

                <TextBlock Text="Юридический адрес *" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="LegalAddressTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="Email" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="EmailTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <TextBlock Text="Телефон" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="PhoneTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Сохранить" 
                           x:Name="SaveButton" 
                           Style="{StaticResource DarkButtonStyle}"
                           Click="SaveButton_Click"
                           Margin="0,0,10,0"/>
                    <Button Content="Отмена" 
                           x:Name="CancelButton" 
                           Style="{StaticResource DarkButtonStyle}"
                           Click="CancelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
