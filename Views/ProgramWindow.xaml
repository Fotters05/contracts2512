<Window x:Class="Contract2512.Views.ProgramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Программа обучения" Height="580" Width="680"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Margin="0,10"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameTextBox" Margin="0,5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Формат:" VerticalAlignment="Center" Margin="0,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="FormatTextBox" Margin="0,5"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Вид программы:" VerticalAlignment="Center" Margin="0,10"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" x:Name="ProgramViewComboBox" DisplayMemberPath="Name" Margin="0,5"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Часы:" VerticalAlignment="Center" Margin="0,10"/>
                    <TextBox Grid.Row="3" Grid.Column="1" x:Name="HoursTextBox" Margin="0,5"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Количество уроков:" VerticalAlignment="Center" Margin="0,10"/>
                    <TextBox Grid.Row="4" Grid.Column="1" x:Name="LessonsCountTextBox" Margin="0,5"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Цена:" VerticalAlignment="Center" Margin="0,10"/>
                    <TextBox Grid.Row="5" Grid.Column="1" x:Name="PriceTextBox" Margin="0,5"/>

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Изображение:" VerticalAlignment="Center" Margin="0,10"/>
                    <Grid Grid.Row="6" Grid.Column="1" Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="ImageTextBox" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" x:Name="BrowseImageButton" Content="Обзор..." 
                               Width="100" Height="25" Click="BrowseImageButton_Click"/>
                    </Grid>

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Превью:" VerticalAlignment="Top" Margin="0,10"/>
                    <Border Grid.Row="7" Grid.Column="1" BorderBrush="Gray" 
                           BorderThickness="1" Margin="0,5" Padding="5">
                        <Image x:Name="PreviewImage" Height="150" Stretch="Uniform"/>
                    </Border>

                </Grid>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Width="120" Height="30" 
                   Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="100" Height="30" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
