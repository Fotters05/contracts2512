<ui:FluentWindow x:Class="Contract2512.Views.WorkloadScheduleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Настройка расписания учебной нагрузки"
        WindowStartupLocation="CenterOwner"
        Height="600"
        Width="900"
        ExtendsContentIntoTitleBar="True">

    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackground" StartPoint="0 0" EndPoint="1 1">
            <GradientStop Color="#1E1B4B" Offset="0"/>
            <GradientStop Color="#0F172A" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="TextPrimary" Color="White"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1E293B" Opacity="0.32"/>
        <SolidColorBrush x:Key="BorderColor" Color="#475569"/>

        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#1E293B" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Background="Transparent"
                                          Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#4B5563" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <SolidColorBrush Color="#6B7280" Opacity="0.8"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#312E81">
            <Grid MouseDown="TitleBar_MouseDown">
                <TextBlock Text="Настройка расписания учебной нагрузки"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="12,0"
                           FontSize="14"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Margin="20"
                    Background="{StaticResource CardBackground}"
                    CornerRadius="12"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок -->
                    <TextBlock Grid.Row="0"
                               Text="Настройка расписания"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"
                               Margin="0,0,0,10"/>

                    <!-- Информация о договоре -->
                    <Border Grid.Row="1"
                            Background="#1E293B"
                            Opacity="0.5"
                            CornerRadius="8"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            Padding="15"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock x:Name="ContractInfoTextBlock"
                                       FontSize="14"
                                       Foreground="{StaticResource TextSecondary}"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <!-- Расписание по дням недели -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,20">
                        <TextBlock Text="Расписание занятий:"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   Margin="0,0,0,5"/>
                        
                        <TextBlock Text="Формат времени: 10:00-10:45, 11:00-11:45 (каждый интервал = 1 акад. час)"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,0,0,15"/>

                        <!-- Понедельник -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="MondayCheckBox"
                                      Content="Понедельник"
                                      Grid.Column="0"
                                      Foreground="{StaticResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Checked="DayCheckBox_Changed"
                                      Unchecked="DayCheckBox_Changed"/>
                            <TextBox x:Name="MondayTimeTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     IsEnabled="False"/>
                        </Grid>

                        <!-- Вторник -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="TuesdayCheckBox"
                                      Content="Вторник"
                                      Grid.Column="0"
                                      Foreground="{StaticResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Checked="DayCheckBox_Changed"
                                      Unchecked="DayCheckBox_Changed"/>
                            <TextBox x:Name="TuesdayTimeTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     IsEnabled="False"/>
                        </Grid>

                        <!-- Среда -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="WednesdayCheckBox"
                                      Content="Среда"
                                      Grid.Column="0"
                                      Foreground="{StaticResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Checked="DayCheckBox_Changed"
                                      Unchecked="DayCheckBox_Changed"/>
                            <TextBox x:Name="WednesdayTimeTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     IsEnabled="False"/>
                        </Grid>

                        <!-- Четверг -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ThursdayCheckBox"
                                      Content="Четверг"
                                      Grid.Column="0"
                                      Foreground="{StaticResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Checked="DayCheckBox_Changed"
                                      Unchecked="DayCheckBox_Changed"/>
                            <TextBox x:Name="ThursdayTimeTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     IsEnabled="False"/>
                        </Grid>

                        <!-- Пятница -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="FridayCheckBox"
                                      Content="Пятница"
                                      Grid.Column="0"
                                      Foreground="{StaticResource TextPrimary}"
                                      VerticalAlignment="Center"
                                      Checked="DayCheckBox_Changed"
                                      Unchecked="DayCheckBox_Changed"/>
                            <TextBox x:Name="FridayTimeTextBox"
                                     Grid.Column="1"
                                     Style="{StaticResource DarkTextBoxStyle}"
                                     IsEnabled="False"/>
                        </Grid>

                        <!-- Валидация -->
                        <TextBlock x:Name="ValidationTextBlock"
                                   FontSize="13"
                                   Foreground="#EF4444"
                                   TextWrapping="Wrap"
                                   Margin="0,10,0,0"
                                   Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Кнопки -->
                    <StackPanel Grid.Row="3"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="Сохранить"
                                x:Name="SaveButton"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="SaveButton_Click"
                                Margin="0,0,10,0"/>
                        <Button Content="Отмена"
                                Style="{StaticResource DarkButtonStyle}"
                                Click="CancelButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
