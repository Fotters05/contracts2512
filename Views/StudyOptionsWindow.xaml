<ui:FluentWindow x:Class="Contract2512.Views.StudyOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Редактирование опций ДОП 1.4"
        Height="700"
        Width="1080"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True">
    <Window.Resources>
        <LinearGradientBrush x:Key="AppBackground" StartPoint="0 0" EndPoint="1 1">
            <GradientStop Color="#1E1B4B" Offset="0"/>
            <GradientStop Color="#0F172A" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="TextPrimary" Color="White"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="PanelBackground" Color="#1E293B" Opacity="0.3"/>
        <SolidColorBrush x:Key="DataGridBackground" Color="#1E293B" Opacity="0.3"/>
        <SolidColorBrush x:Key="DataGridRowBackground" Color="#1E293B" Opacity="0.3"/>
        <SolidColorBrush x:Key="DataGridAlternatingRowBackground" Color="#0F172A" Opacity="0.3"/>
        <SolidColorBrush x:Key="BorderColor" Color="#475569"/>

        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          Opacity="0.45"
                          BlurRadius="25"
                          ShadowDepth="4"/>

        <Style x:Key="DarkGrayButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#4B5563" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#1E293B" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="DarkDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="{StaticResource DataGridRowBackground}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource DataGridAlternatingRowBackground}"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
        </Style>

        <Style x:Key="DarkDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#334155" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <Style x:Key="DarkDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderBrush" Value="#475569"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <Style x:Key="DarkDataGridRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#55334155"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#664C46E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource AppBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown">
            <TextBlock Text="Редактирование опций ДОП 1.4"
                       Foreground="{StaticResource TextPrimary}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="-" Width="46" Height="32" Background="Transparent" Foreground="White" BorderThickness="0" Click="MinimizeButton_Click"/>
                <Button x:Name="MaximizeRestoreButton" Content="□" Width="46" Height="32" Background="Transparent" Foreground="White" BorderThickness="0" Click="MaximizeButton_Click"/>
                <Button Content="×" Width="46" Height="32" Background="Transparent" Foreground="White" BorderThickness="0" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="16" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Background="{StaticResource PanelBackground}" Margin="0,0,0,12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Ключ" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,10"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="OptionKeyTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,16,10"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Порядок" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,10"/>
                    <TextBox Grid.Row="0" Grid.Column="3" x:Name="SortOrderTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Название" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="NameTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,16,10"/>
                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Часы/нед" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,10"/>
                    <TextBox Grid.Row="1" Grid.Column="3" x:Name="HoursTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,12,10"/>
                    <TextBlock Grid.Row="1" Grid.Column="4" Text="Недель" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="0,0,12,10"/>
                    <TextBox Grid.Row="1" Grid.Column="5" x:Name="WeeksTextBox" Style="{StaticResource DarkTextBoxStyle}" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Текст" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Top" Margin="0,4,12,0"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="5" x:Name="TextTextBox" Style="{StaticResource DarkTextBoxStyle}" Height="90" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="Сохранить" Style="{StaticResource DarkGrayButton}" Click="SaveButton_Click" Margin="0,0,8,0"/>
                <Button Content="Новая опция" Style="{StaticResource DarkGrayButton}" Click="NewButton_Click" Margin="0,0,8,0"/>
                <Button Content="Удалить" Style="{StaticResource DarkGrayButton}" Click="DeleteButton_Click" Margin="0,0,8,0"/>
                <Button Content="Обновить" Style="{StaticResource DarkGrayButton}" Click="RefreshButton_Click"/>
            </StackPanel>

            <Border Grid.Row="2"
                    Background="{StaticResource DataGridBackground}"
                    CornerRadius="8"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    ClipToBounds="True"
                    Effect="{StaticResource CardShadow}">
                <DataGrid x:Name="OptionsDataGrid" Style="{StaticResource DarkDataGridStyle}" SelectionChanged="OptionsDataGrid_SelectionChanged">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DarkDataGridColumnHeaderStyle}"/>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource DarkDataGridCellStyle}"/>
                    </DataGrid.CellStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource DarkDataGridRowStyle}"/>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ключ" Binding="{Binding OptionKey}" Width="130"/>
                        <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Часы" Binding="{Binding HoursPerWeek}" Width="80"/>
                        <DataGridTextColumn Header="Недель" Binding="{Binding WeeksDuration}" Width="80"/>
                        <DataGridTextColumn Header="Порядок" Binding="{Binding SortOrder}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
