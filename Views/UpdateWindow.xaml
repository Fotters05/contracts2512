<Window x:Class="Contract2512.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Доступно обновление" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🎉 Доступна новая версия!" 
                       FontSize="20" 
                       FontWeight="Bold"
                       Foreground="#2196F3"/>
            <TextBlock x:Name="VersionText" 
                       Text="Версия: 1.0.1" 
                       FontSize="14" 
                       Foreground="#666"
                       Margin="0,5,0,0"/>
            <TextBlock x:Name="DateText" 
                       Text="Опубликовано: 13.03.2026" 
                       FontSize="12" 
                       Foreground="#999"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Что нового -->
        <TextBlock Grid.Row="1" 
                   Text="Что нового:" 
                   FontSize="14" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,10"/>

        <!-- Описание изменений -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ReleaseNotesText" 
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="#333"/>
            </ScrollViewer>
        </Border>

        <!-- Прогресс бар -->
        <StackPanel Grid.Row="3" Margin="0,15,0,0" x:Name="ProgressPanel" Visibility="Collapsed">
            <TextBlock x:Name="ProgressText" 
                       Text="Скачивание обновления..." 
                       FontSize="12" 
                       Foreground="#666"
                       Margin="0,0,0,5"/>
            <ProgressBar x:Name="ProgressBar" 
                         Height="20" 
                         Minimum="0" 
                         Maximum="100"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button x:Name="UpdateButton" 
                    Content="Обновить сейчас" 
                    Width="130" 
                    Height="35"
                    Background="#4CAF50" 
                    Foreground="White"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="UpdateButton_Click"
                    Margin="0,0,10,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5"
                                            Padding="10,5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button x:Name="LaterButton" 
                    Content="Позже" 
                    Width="100" 
                    Height="35"
                    Background="#E0E0E0" 
                    Foreground="#333"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="LaterButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5"
                                            Padding="10,5">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                         VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
