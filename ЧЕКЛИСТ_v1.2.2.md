# ✅ Чеклист: Выпуск версии 1.2.2

## Что уже сделано автоматически:
- ✅ Исправлен `Services/AutoUpdateService.cs` (использует рефлексию)
- ✅ Изменена версия в `Contract2512.csproj` на 1.2.2
- ✅ Создан файл `СРОЧНО_ПРОЧИТАЙ.md` с инструкциями
- ✅ Создан файл `ИСПРАВЛЕНИЯ_v1.2.2.md` с описанием изменений

## Что нужно сделать вручную:

### 1. Проверь компиляцию (ОБЯЗАТЕЛЬНО!)
```powershell
dotnet clean
dotnet build
```

**Ожидаемый результат:** Сборка успешна, без ошибок

**Если есть ошибки:** ОСТАНОВИ и напиши мне!

---

### 2. Удали сломанный тег v1.2.1
```powershell
git tag -d v1.2.1
git push origin :refs/tags/v1.2.1
```

**Ожидаемый результат:** 
```
Deleted tag 'v1.2.1' (was xxxxxxx)
To https://github.com/Fotters05/contracts2512.git
 - [deleted]         v1.2.1
```

---

### 3. Закоммить исправления
```powershell
git add Services/AutoUpdateService.cs Contract2512.csproj СРОЧНО_ПРОЧИТАЙ.md ИСПРАВЛЕНИЯ_v1.2.2.md ЧЕКЛИСТ_v1.2.2.md
git commit -m "Fix: Use reflection in AutoUpdateService to avoid WPF temp project errors"
```

**Ожидаемый результат:**
```
[master xxxxxxx] Fix: Use reflection in AutoUpdateService to avoid WPF temp project errors
 5 files changed, XXX insertions(+), XXX deletions(-)
```

---

### 4. Создать тег v1.2.2 и запушить
```powershell
git tag v1.2.2
git push origin master
git push origin v1.2.2
```

**Ожидаемый результат:**
```
To https://github.com/Fotters05/contracts2512.git
   xxxxxxx..yyyyyyy  master -> master
 * [new tag]         v1.2.2 -> v1.2.2
```

---

### 5. Подожди 5-10 минут
GitHub Actions должен собрать релиз. Можешь следить за процессом:
https://github.com/Fotters05/contracts2512/actions

**Ожидаемый результат:** Зелёная галочка ✅ напротив workflow "Build and Release"

---

### 6. Проверь релиз на GitHub
Зайди на: https://github.com/Fotters05/contracts2512/releases

**Ожидаемый результат:** 
- Релиз v1.2.2 создан
- Есть файл `Contract2512Setup.exe`
- Есть файлы `.nupkg` и `RELEASES`

---

### 7. Скачай и установи v1.2.2
1. Скачай `Contract2512Setup.exe` из релиза v1.2.2
2. Запусти установщик
3. Дождись завершения установки

**Ожидаемый результат:** 
- Приложение установлено
- Создан ярлык на рабочем столе
- При запуске показывается версия 1.2.2

---

### 8. Протестируй автообновление

#### 8.1. Создай тестовую версию v1.2.3

Открой `Contract2512.csproj` и измени:
```xml
<Version>1.2.3</Version>
<AssemblyVersion>1.2.3.0</AssemblyVersion>
<FileVersion>1.2.3.0</FileVersion>
```

Затем:
```powershell
git add Contract2512.csproj
git commit -m "Bump version to 1.2.3 for testing auto-update"
git tag v1.2.3
git push origin master
git push origin v1.2.3
```

#### 8.2. Подожди 5-10 минут
Пока GitHub Actions соберёт v1.2.3

#### 8.3. Запусти установленную версию 1.2.2
Просто запусти приложение через ярлык

**Ожидаемый результат:**
- Через несколько секунд появится окно "Доступно обновление"
- Показывается версия 1.2.3
- Кнопка "Обновить"

#### 8.4. Нажми "Обновить"
**Ожидаемый результат:**
- Прогресс-бар загрузки
- Сообщение "Обновление установлено"
- Кнопка "Перезапустить"

#### 8.5. Нажми "Перезапустить"
**Ожидаемый результат:**
- Приложение перезапускается
- Теперь показывается версия 1.2.3

---

## 🎉 Если всё прошло успешно:
Автообновление работает! Теперь при каждом новом релизе пользователи будут автоматически получать обновления.

## ❌ Если что-то пошло не так:
Напиши мне на каком шаге возникла проблема и что показывает ошибка.

---

## Важные замечания:

1. **Всегда проверяй компиляцию** перед созданием тега
2. **Используй только установленную версию** для тестирования автообновления (не Debug build!)
3. **Подожди 5-10 минут** после push тега - GitHub Actions нужно время на сборку
4. **Проверяй GitHub Actions** - там видно все ошибки сборки
5. **Файл Setup называется `Contract2512Setup.exe`** (не просто `Setup.exe`)
